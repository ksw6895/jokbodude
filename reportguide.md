당신은 대규모 서비스를 설계하고 운영하는 데 전문성을 가진 최고 수준의 시스템 아키텍트이자 코드 분석가입니다. 현재 소규모 사용자를 대상으로 운영 중인 "JokboDude" 프로젝트를 수백 명 이상의 동시 사용자를 감당할 수 있는 안정적이고 확장 가능한 서비스로 성장시키려 합니다.

당신의 임무는 제공된 전체 코드베이스와 관련 문서를 면밀히 분석하여, 서비스 확장 시 발생할 수 있는 잠재적 문제점을 **"코드 내부 문제"**와 **"코드 외부 환경 문제"** 두 가지 관점에서 종합적으로 진단하고, 이에 대한 구체적인 개선 방안을 제시하는 것입니다. 최종 결과물은 다른 개발자들이 명확하게 이해하고 실행할 수 있도록 상세한 설명과 체크리스트를 포함한 한국어로 적힌 마크다운(.md) 형식의 보고서여야 합니다. 

## 1. 분석의 핵심 목표

* **병목 현상 식별**: 사용자가 급증했을 때 시스템의 성능을 저하할 수 있는 부분을 코드와 인프라 전반에 걸쳐 찾아냅니다.
* **안정성 및 복원력 강화**: 특정 컴포넌트의 장애가 전체 서비스 중단으로 이어지지 않도록 시스템의 복원력을 높일 방안을 제시합니다.
* **운영 및 유지보수 효율성 증대**: 서비스 규모가 커져도 최소한의 노력으로 안정적인 업데이트, 모니터링, 유지보수가 가능한 구조를 제안합니다.
* **비용 효율성 고려**: 확장성과 안정성을 확보하면서도 불필요한 비용이 발생하지 않도록 리소스 사용을 최적화할 방안을 모색합니다.

## 2. 분석 보고서 필수 포함 내용

### I. 코드 내부 문제 분석 (Code-Level Scalability)

애플리케이션의 소스 코드를 중심으로, 수백 명의 동시 사용자가 발생시키는 부하를 감당하기 어려운 부분을 심층적으로 분석합니다.

* **동시성 및 병렬 처리**:
    * Celery 워커 설정(`celeryconfig.py`, `tasks.py`)이 다수의 동시 작업을 효율적으로 처리할 수 있는지 검토합니다. (예: `concurrency` 설정, `worker_max_tasks_per_child` 등)
    * `MultiAPIManager`가 API 키를 관리하는 방식이 스레드 안전하며, 병렬 요청 시 발생할 수 있는 경쟁 조건은 없는지 분석합니다.
    * FastAPI의 비동기(`async`) 이점을 최대로 활용하고 있는지, 블로킹 I/O 작업이 이벤트 루프를 막고 있지는 않은지 확인합니다.

* **성능 병목**:
    * `pdf_processor` 모듈의 PDF 파싱, 이미지 추출, 텍스트 처리 로직에서 CPU 또는 메모리 사용량이 과도한 부분을 식별합니다.
    * `StorageManager`를 통해 Redis에 데이터를 읽고 쓰는 과정에서 발생하는 잠재적 성능 저하 요인을 분석합니다. 특히, 큰 파일 데이터를 처리하는 방식의 효율성을 평가합니다.
    * 파일 I/O가 빈번하게 발생하는 구간을 찾아내고, 비동기 I/O나 캐싱을 통해 개선할 수 있는 부분을 제안합니다.

* **리소스 관리**:
    * PDF 문서를 처리하며 생성되는 임시 파일(`tmpdir.py`)과 캐시(`pdf_processor/pdf/cache.py`)가 메모리나 디스크 공간을 고갈시킬 위험은 없는지 분석하고, 효과적인 정리 전략을 제시합니다.
    * 현재의 주기적인 아티팩트 정리 방식(`prune_artifacts.py`)이 대규모 환경에서도 충분한지, 혹은 더 동적인 정리 메커니즘이 필요한지 검토합니다.

* **에러 핸들링 및 복원력**:
    * Gemini API 호출 실패, PDF 파일 손상, Redis 연결 문제 등 외부 의존성 장애 발생 시 시스템이 어떻게 반응하는지 분석합니다. Celery 작업의 재시도 로직(`task_acks_late`, `task_reject_on_worker_lost`)이 적절한지 평가합니다.
    * 특정 사용자의 잘못된 요청이나 대용량 파일 하나가 다른 전체 사용자에게 영향을 미칠 수 있는 "노이지 네이버(Noisy Neighbor)" 문제가 발생할 가능성은 없는지 확인합니다.

### II. 코드 외부 환경 문제 분석 (Environment & DevOps Scalability)

코드가 배포되고 실행되는 전체 인프라와 운영 환경 관점에서 확장성을 분석합니다. `render.yaml`, `R2_MIGRATION.md`, `CONCURRENCY_AND_ID.md` 등의 문서를 주요하게 참고하세요.

* **서버 아키텍처 및 확장성**:
    * `render.yaml`에 정의된 웹 서비스와 워커 서비스가 사용량에 따라 자동으로 확장(Autoscaling)될 수 있는 구조인지, 아니면 수동 개입이 필요한지 분석합니다.
    * Web/Worker 인스턴스 간의 부하 분산 전략과 Celery 큐 분리를 통한 작업 우선순위 처리 방안을 제안합니다. (예: 파일 업로드 큐, PDF 처리 큐, 결과 생성 큐 분리)

* **스토리지 확장성 및 안정성**:
    * 현재 Redis를 파일 저장소, 캐시, 메시지 큐 등 다용도로 사용하는 구조가 대규모 트래픽에서 병목이 될 가능성을 평가합니다. (`R2_MIGRATION.md`를 참고하여 Object Storage로의 전환의 중요성과 현재 구현 상태의 잠재적 문제를 분석)
    * `RENDER_STORAGE_PATH`와 같은 로컬 디스크 의존성이 수평적으로 확장된(stateless) 서버 환경에서 문제를 일으키지 않을지 검토합니다. 모든 상태가 중앙 저장소(Redis, R2)를 통해 공유되는지 확인합니다.

* **배포 및 유지보수**:
    * 새 버전 배포 시 서비스 중단 시간(Downtime)을 최소화할 수 있는 전략(예: Blue-Green, Canary 배포)을 제안합니다.
    * 데이터베이스 스키마나 주요 로직 변경 시 하위 호환성을 유지하며 매끄럽게 업데이트를 진행할 수 있는 방안을 제시합니다.

* **모니터링 및 로깅**:
    * 현재의 로깅 시스템(`logging.py`)이 대규모 환경에서 로그를 중앙에서 수집하고 검색하기에 적합한지 평가합니다.
    * API 응답 시간, Celery 작업 대기열 길이, 에러 발생률, 서버 리소스(CPU, Memory) 사용량 등 핵심 성능 지표(KPI)를 지속적으로 추적하고 임계치 도달 시 알림을 받을 수 있는 모니터링 시스템 구축 방안을 제안합니다.

## 3. 최종 산출물 형식

* **Markdown(.md) 파일**: `Scalability_Analysis_Report.md`
* **구조**:
    * **개요 (Executive Summary)**: 분석의 목적과 핵심 발견 사항을 요약합니다.
    * **I. 코드 내부 문제 분석**:
        * 동시성 및 병렬 처리
        * 성능 병목
        * 리소스 관리
        * 에러 핸들링 및 복원력
        * (각 항목별로 문제점, 예상 영향, 구체적인 개선 권장 사항 포함)
    * **II. 코드 외부 환경 문제 분석**:
        * 서버 아키텍처 및 확장성
        * 스토리지 확장성 및 안정성
        * 배포 및 유지보수
        * 모니터링 및 로깅
        * (각 항목별로 문제점, 예상 영향, 구체적인 개선 권장 사항 포함)
    * **체크리스트 (Action Item Checklist)**: 단기, 중기, 장기로 나누어 실행해야 할 모든 개선 과제를 우선순위에 따라 정리한 체크리스트를 제공합니다.

당신의 깊이 있는 분석과 통찰력이 이 프로젝트가 성공적으로 성장하는 데 결정적인 역할을 할 것입니다. 코드의 세세한 라인 수정보다는, 전체적인 구조와 아키텍처의 문제점을 지적하고 큰 틀에서의 개선 방향을 제시하는 데 집중해 주십시오.
