# Streaming Token Accounting Implementation Guide

이 문서는 Gemini API 사용량을 기반으로 한 JD 토큰 실시간 차감 로직을 도입하기 위한 개발 지침입니다. 실제 구현을 시작하기 전에 아래 요구사항을 충족하도록 설계를 점검해 주세요.

## 1. 불필요한 문서 정리
- 현재 레포지토리에 존재하는 임시/가이드라인 미완성 문서(`*.md`, `prompt_output.txt` 등)를 재검토하고, 운영/개발에 불필요한 파일은 삭제합니다.
- 삭제 전에는 실제로 사용 중인지 각 문서의 작성 목적과 최신성(업데이트 날짜, README/문서 링크 등을 통해)을 반드시 확인합니다.
- 삭제한 파일은 커밋 메시지 및 PR 설명에 정리하여, 팀원들이 제거 내역을 빠르게 파악할 수 있도록 합니다.

## 2. 실시간 스트리밍 차감 도입
1. **토큰 사용량 스트리밍 수집**
   - `GeminiAPIClient` 레이어에서 스트리밍 응답을 처리할 때, Gemini가 반환하는 토큰 사용량(입력/출력)을 청크 단위로 누적합니다.
   - Gemini API가 실시간 사용량 메타데이터를 제공하지 않는 경우, 각 스트리밍 청크마다 예상 토큰 수를 계산하거나, API에서 제공하는 `usage_metadata`를 주기적으로 요청해 누적치를 파악할 수 있는지 확인합니다.

2. **JD 토큰 환산 로직 조정**
   - `BillingConverter.api_to_jd` 로직을 수정하여 "Gemini 토큰 100개당 JD 토큰 1개" 비율을 적용합니다. 즉, `ceil(total_token_count / 100 × multiplier)` 방식으로 변경합니다.
   - 스트리밍 중에는 현재까지 누적된 Gemini 토큰 수를 100으로 나눈 몫이 증가하는 순간마다 JD 토큰을 차감합니다. 예: 누적 199개의 Gemini 토큰이면 1 JD 차감, 200개가 되면 2 JD 차감.
   - 기존 환경변수(`JD_TOKEN_MULTIPLIER_*`)도 동일하게 작동하도록 배율 적용 시점을 조정합니다.

3. **Redis/스토리지 반영**
   - `BaseAnalyzer.upload_and_analyze` 또는 해당 스트리밍 파이프라인에서, 누적 사용량을 Redis 등 상태 저장소에 저장하고, 스트리밍 청크 수신 시마다 차감 내역을 갱신합니다.
   - Redis에는 작업별 "총 사용 Gemini 토큰"과 "차감된 JD 토큰"을 모두 기록하여, 추후 롤백이나 감사 로그 작성에 활용할 수 있게 합니다.

## 3. 잔액 고갈 시 처리 규칙
1. **잔액 모니터링**
   - 실시간 차감 로직에 따라, JD 토큰 잔액이 0이 되었는지 매 청크마다 확인합니다.
   - 잔액이 부족한 경우 즉시 스트리밍을 중단하고, Gemini API 호출을 `cancel/close` 하여 추가 비용이 발생하지 않도록 합니다.

2. **사용자 통지 및 롤백**
   - 해당 작업을 실패 상태로 마킹하고, 사용자에게 "토큰 부족" 메시지를 전송합니다 (FastAPI 응답, WebSocket, Celery 결과 등 현재 사용 중인 통신 경로에 맞게 구현).
   - 이번 작업에서 이미 차감했던 JD 토큰은 전액 복구합니다. Redis에 기록한 누적 사용량을 활용하여 사용자 계정의 JD 잔액을 롤백합니다.
   - 실패한 작업은 재시작할 수 있도록 별도 상태(예: `failed_insufficient_tokens`)를 기록하여 운영자가 구분할 수 있게 합니다.

3. **후속 처리**
   - 실패 기록과 함께 로그/모니터링 시스템에 토큰 부족 이벤트를 남겨, 추후 사용자 지원 및 과금 검증에 활용합니다.
   - 프론트엔드(또는 API 응답 스키마)에서도 "잔액 부족" 상태를 명확히 표시하도록 계약을 업데이트합니다.

## 4. 장기 `unknown` 상태 Job 방지
- 현재 Redis/DB 상에 `unknown` 상태로 장기간 남아 있는 Job이 다수 발생하고 있으므로, 스트리밍 차감 로직과 함께 다음을 점검합니다.
  - Job 상태 전이를 담당하는 Celery 태스크, 웹 서버 상태 업데이트 핸들러, Redis 스토리지 레이어에서 "시작 → 진행 중 → 완료/실패" 플로우가 정상적으로 호출되는지 추적 로그를 보강합니다.
  - 예외 발생 시 `unknown` 상태를 남기지 않도록 명시적으로 실패 상태로 전환하고, 사용자에게 실패 사유를 반환합니다.
  - 타임아웃, 워커 재시작, 네트워크 오류 등 비정상 종료 시에도 Job 정리가 이뤄지도록 보완 스케줄러나 청소 루틴을 추가합니다.
  - 장기간 `unknown` 상태로 남아 있는 Job을 자동으로 정리하는 클리어링 시스템을 도입하여, 일정 시간 초과 시 관련 리소스를 정리하고 사용자 상태를 명확한 실패 코드로 갱신합니다.
  - Cloudflare R2와 같은 외부 스토리지에서 객체가 자동 삭제되지만 메타데이터는 남아 있는 상황이 `unknown` 상태의 원인일 수 있으므로, 스토리지/DB 동기화 로직을 점검해 "삭제된 객체 → Job 상태 업데이트" 플로우를 보완하고 애초에 `unknown` 상태가 생성되지 않도록 합니다.

## 5. 테스트 및 검증
- 스트리밍 응답 테스트를 위한 통합/단위 테스트를 추가하여, 잔액이 충분한 경우와 부족한 경우 모두 검증합니다.
- 테스트 시에는 Redis, Celery, FastAPI 서버 등 관련 구성 요소를 가능한 한 실제와 유사한 환경에서 구동하여 동시성 및 롤백 로직을 검증합니다.
- 변경 사항에 대해 README 또는 운영 문서에 스트리밍 과금 방식 변경 내용을 정리합니다.

위 지침을 참고하여 실제 구현을 진행해 주세요. 개발 도중 추가적인 예외 사항이 발견되면 본 가이드를 업데이트하거나 별도의 기술 결정 기록(TDR)을 남겨 공유합니다. 필요할 경우 코드 리팩토링도 점검 차원에서 병행해 주세요.
